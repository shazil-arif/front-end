/**
 * Object:
 * key value pairs
 * not contiguous in memory
 * not iterable
 * 
 * Array
 * ordered list of items. any item can be retrieved if its position in the list is known
 * contiguous in memory
 * iterable
 * Under the hood it is an object! but we ignore this, abstract out the specifics and just 
 * think of it as regular array.
 */