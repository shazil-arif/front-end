/**
 * part of the array class.
 * available for any array
 * 
 * takes one callback with one parameter representing the current item in the array
 *
 * Used to iterate over each array element and do things like: print out value, store into new array etc
 */